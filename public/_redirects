# 1. Remove /blog/ prefix on new domain: /blog/YYYY* -> /YYYY* (301 redirect)
/blog/20* /20:splat 301

# 2. Serve root slugs from /blog/ internally: /YYYY* -> /blog/YYYY* (200 rewrite)
# This must come AFTER the redirect above to avoid loops
/20* /blog/20:splat 200

# Legacy slug redirect (keep existing)
/blog/2026-03-03-kyc-providers-become-reputation-banks/* /blog/2026-04-15-when-banks-become-reputation-banks/:splat 301
